node_modules/
dist/
.DS_Store
*.local

# Capacitor copies the built web app in here on every sync, so it is
# generated output rather than source. The rest of ios/ IS source and must
# be committed — it holds the Xcode project and signing settings.
ios/App/App/public/
ios/App/Pods/
ios/App/App.xcworkspace/xcuserdata/
ios/App/App.xcodeproj/xcuserdata/
ios/DerivedData/

.netlify/
.vercel/
